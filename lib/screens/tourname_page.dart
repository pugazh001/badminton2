import 'package:flutter/material.dart';


class TournamentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tournament'),
      ),
      body: Center(
        child: Text('Tournament Page'),
      ),
    );
  }
}
