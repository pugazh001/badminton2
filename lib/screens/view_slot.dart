import 'package:flutter/material.dart';

class ViewBookedSlotsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('View Booked Slots'),
      ),
      body: Center(
        child: Text('View Booked Slots Page'),
      ),
    );
  }
}
